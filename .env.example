# Create a Supabase project, then copy its URL and publishable (anon) key here.
# These values are safe to expose to the browser; access to the data itself is
# enforced by the Row Level Security policies in supabase/migrations/.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
